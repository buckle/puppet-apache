# file managed by puppet
<% if scope.lookupvar('sslonly') != true -%>
<%= scope.function_template(["apache/vhost.erb"]) -%>

<% end -%>
<VirtualHost<% "scope.lookupvar('apache::vhost_ssl::sslports')".each do |port| -%> <%= port %><% end -%>>
  ServerName <%= name %>

<% aliases.each do |serveralias| -%>
  ServerAlias <%= serveralias %>
<% end -%>

  DocumentRoot <%= documentroot %>

  LogLevel warn

  ErrorLog <%= wwwroot %>/<%= name %>/logs/error.log
  CustomLog <%= wwwroot %>/<%= name %>/logs/access.log combined

  Include <%= wwwroot %>/<%= name%>/conf/*.conf

  <Directory <%= documentroot %>>
    Options Indexes FollowSymLinks MultiViews
    AllowOverride <%=allow_override %>
    Order allow,deny
    allow from all
  </Directory>

  SSLEngine On
  SSLCipherSuite ALL:!aNULL:!ADH:!eNULL:!LOW:!EXP:RC4+RSA:+HIGH:+MEDIUM
  SSLCertificateFile <%= scope.lookupvar('certfile') %>
  SSLCertificateKeyFile <%= scope.lookupvar('certkeyfile') %>
  SSLCACertificateFile <%= scope.lookupvar('cacertfile') %>
<% if has_variable?("scope.lookupvar('certchainfile')") -%>
  SSLCertificateChainFile <%= scope.lookupvar('certchainfile') %>
<% end -%>

<% if cgipath -%>
  ScriptAlias /cgi-bin/ <%= cgipath %>
  <Directory <%= cgipath %>>
    Options +ExecCGI
    AddHandler cgi-script .cgi
  </Directory>
<% end -%>

  ServerSignature Off

</VirtualHost>
